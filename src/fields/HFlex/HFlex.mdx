import { Meta, Canvas } from '@storybook/addon-docs/blocks';

import * as Stories from './HFlex.stories';

<Meta of={Stories} />

# HFlex

This is a type that can contains other types using its prop `children`

## Example

<Canvas of={Stories.Base} />
